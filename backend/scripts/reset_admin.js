const mysql = require('mysql2/promise'); const bcrypt = require('bcryptjs'); async function run() { const pool = mysql.createPool({ host: 'localhost', user: 'root', password: 'Heyywillz@911', database: 'sellout_db' }); const hash = await bcrypt.hash('admin123', 10); await pool.query('UPDATE users SET password = ? WHERE email = ?', [hash, 'admin@sellout.gh']); console.log('Password updated to admin123'); process.exit(0); } run();
